import React from 'react';
import { View, Text } from 'react-native';

class App extends React.Component {
    render() {
        return (
            <View style={{marginTop: 200}}>
                <Text>Let's start our code here!</Text>
            </View>
        );
    }
};

export default App;